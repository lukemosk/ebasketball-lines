{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run ETL",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/etl.py",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}